<h3 class="sub-title mb-3">Study & work</h3>

<div class="col">
  <div class="timeline-body">
    {%- for item in timeline -%}
    <div class="timeline-item">
      <div class="content">
        <h4 class="sub-title">{{- item.title -}}, <a href="{{ item.href }}">{{ item.company -}}</a></h4>
        <p class="date">{{- item.from }} — {{ item.to -}}</p>
        <p class="description">{{ item.description }}</p>
      </div>
    </div>
    {%- endfor -%}
  </div>
</div>
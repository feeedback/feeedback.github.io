{%- if include.block -%}
  {%- set block_style = "btn-block" -%}
{%- else -%}
  {%- set block_style = "" -%}
{%- endif -%}

<a class="m-1 btn btn-{{ include.style | default: 'outline-primary' }} btn-{{ include.size | default: 'md' }} {{ block_style }}" href="{{ include.link | default: '#' }}">
  {{- include.text | default: "Button" -}}
</a>